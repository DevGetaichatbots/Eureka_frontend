# Server-Rendered Templates
This directory is reserved for optional Jinja2 server-rendered templates per Section 11 of the Technical Build Document.
When using the Next.js frontend (located in `/frontend`), API routes return JSON directly to the frontend viewer.
